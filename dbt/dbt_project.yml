name: hdb_resale
version: '1.0.0'
config-version: 2

profile: hdb_resale

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hdb_resale:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
